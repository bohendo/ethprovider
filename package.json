{
  "name": "fullnode",
  "version": "1.0.0",
  "description": "Tools for maintaining an Ethereum full node",
  "main": "",
  "scripts": {
    "watch": "webpack --watch --config webpack.config.js",
    "start": "make && docker stack deploy -c docker-compose.yml fullnode",
    "reset": "docker stack rm fullnode && touch js/*.js && npm start",
    "build": "webpack --config webpack.config.js",
    "deploy": "bash deploy.sh"
  },
  "author": "Bo Henderson <twitter.com/bohendo>",
  "license": "GPLv3",
  "dependencies": {},
  "devDependencies": {
    "babel-core": "^6.26.0",
    "babel-loader": "^7.1.2",
    "babel-preset-es2015": "^6.24.1",
    "webpack": "^3.10.0"
  }
}
